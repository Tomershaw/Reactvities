name: Trigger auto deployment for reactivities-api-v2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/reactivities-api-v2-AutoDeployTrigger-bedb642f-3444-4be0-8ea6-ae9364cfe032.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.REACTIVITIESAPIV2_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.REACTIVITIESAPIV2_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.REACTIVITIESAPIV2_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: 
          registryUsername: ${{ secrets.REACTIVITIESAPIV2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.REACTIVITIESAPIV2_REGISTRY_PASSWORD }}
          containerAppName: reactivities-api-v2
          resourceGroup: myReactivitiesGroup
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


